# ===========================================
# AI Code Review Bot — Environment Variables
# ===========================================

# --- GitHub App ---
# Create at: https://github.com/settings/apps/new
# Permissions needed: pull_requests (read/write), contents (read), metadata (read)
# Events: pull_request, installation
GITHUB_APP_ID=
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
GITHUB_WEBHOOK_SECRET=           # Generate: openssl rand -hex 32
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# --- Database ---
# Local Docker Compose uses port 5433 to avoid conflicts
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/ai_code_review

# --- Valkey (Redis-compatible) ---
# Local Docker Compose uses port 6380 to avoid conflicts
VALKEY_URL=valkey://localhost:6380

# --- Anthropic ---
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# --- App ---
NEXTAUTH_SECRET=                 # Generate: openssl rand -hex 32
NEXTAUTH_URL=http://localhost:3000
NODE_ENV=development
